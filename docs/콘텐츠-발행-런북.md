# 콘텐츠 발행 런북

`medirok_콘텐츠_발행팩.md` §6 체크리스트를 실제 실행 절차로 옮긴 문서. 신규 매거진 발행 시 이 순서를 따른다.

> 병원 블로그(`/blog`, 네이버 이식) 발행은 절차가 대부분 같고 **다른 점만** §9에 정리했다.

## 0. 전제

- 콘텐츠는 **DB에만** 존재한다. MDX/파일 없음. 발행 경로는 시드 스크립트(`scripts/seed-magazine-*.ts`) 또는 `/admin`.
- **드래프트 상태가 없다.** 시드 실행 = 즉시 라이브.
- `.env.local`은 **운영 Neon DB**를 가리킨다. 시드는 반드시 `getSeedPayload()`(`scripts/seed-payload.ts`)를 경유해 `PAYLOAD_DB_PUSH=false`로 스키마 push를 차단할 것.
- 매거진 상세·사이트맵은 `force-dynamic`이라 **시드 즉시 반영**된다(배포 불필요).

## 1. 원고 작성

1. `docs/매거진-*.md`에 원고·필드·검증 근거를 기록
2. **사실 원칙**: 병원 관련 서술은 `scripts/seed-yeon.ts`·`scripts/seed-dod.ts`의 검증분만 사용. 미확인 수치·밀집도·비율·최상급 표현은 쓰지 않는다.
3. 가격은 `prices: []`인 병원이면 `priceTable` 미사용 (추정가 미조작).

### 마크다운 주의 — 볼드가 안 닫히는 경우

닫는 `**` **앞이 구두점이고 뒤가 한글/영문**이면 CommonMark의 right-flanking 조건을 만족하지 못해 강조가 닫히지 않고 `**`가 원문 그대로 노출된다.

```
✗ **... 지정하는 '첨단재생의료실시기관'**에서만 가능합니다   ← 닫는 ** 앞이 ' , 뒤가 한글
✓ **... 지정하는 첨단재생의료실시기관**에서만 가능합니다
✓ **목업(Mock-up)** — 임시 재료로 ...                      ← 닫는 ** 뒤가 공백이면 OK
```

2026-07 W3 발행 때 원고 03에서 실제로 발생했고 운영 스모크 e2e가 잡았다.

### 링크 정책

본문 내부 링크는 **루트 상대 경로**로 쓴다.

```
✓ [예온치과병원](/hospital/예온치과병원)
✗ [예온치과병원](https://www.medirok.com/hospital/예온치과병원)
```

`src/components/Markdown.tsx`가 자사 호스트를 내부로 판정하도록 고쳐져 절대 URL도 이제는 follow 처리되지만, 상대 경로가 정공법이다. 병원 공식 사이트 등 진짜 외부 링크는 절대 URL로 두면 `nofollow`+`_blank`가 붙는다.

## 2. 시드 스크립트

`scripts/seed-magazine-geomdan.ts`를 템플릿으로 복사 → `package.json`에 `seed:magazine-<name>` 추가.

`upsertWithRefs`가 slug를 FK로 변환하므로 참조 대상(병원·진료과·지역)이 **먼저 존재해야** 한다. 없으면 에러가 아니라 `⚠ 매칭 실패` 경고만 나고 관계가 빈다 — **로그를 반드시 눈으로 확인**.

- 진료과 slug는 영문(`dental`, `dermatology`), 지역 slug는 한국어(`서구`, `강남구`, `인천`)
- `type`은 기존 enum만(`article|qna|regional|interview|case`). 새 값은 Payload 마이그레이션 필요
- `category`는 자유 텍스트 — `醫錄 인증`처럼 enum에 없는 표기는 여기에 넣는다

## 3. 로컬 검증 (운영 반영 전 필수)

```bash
npm run typecheck && npm run build

npm run e2e:db:up
npm run e2e:seed
node --env-file=.env.e2e --import tsx/esm scripts/seed-yeon.ts    # FK 대상 (e2e 픽스처에 없음)
node --env-file=.env.e2e --import tsx/esm scripts/seed-dod.ts
node --env-file=.env.e2e --import tsx/esm scripts/seed-magazine-<name>.ts

# dev 서버는 .env.local(운영)을 자동 로드하므로 셸에 e2e 값을 export해 덮어쓴다
set -a; . ./.env.e2e; set +a; npm run dev
```

> `node --env-file=... next dev`는 동작하지 않는다 (`--env-file= is not allowed in NODE_OPTIONS`). 위처럼 셸 export를 쓸 것. 프로세스 env가 dotenv 파일보다 우선한다.

확인 항목:
- `/magazine/<slug>` 200, shortAnswer 박스·표·FAQ 아코디언 렌더
- JSON-LD 3종 — `Article`(qna면 `QAPage`) + `FAQPage` + `BreadcrumbList`
- 본문 내부 링크에 `rel="nofollow"`가 **없을** 것
- "관련 메디록 의원" 카드 · "관련 매거진" 자동 노출
- `/hospital/<병원>`에 "관련 메디록 매거진" 역링크
- `/sitemap.xml`에 새 URL

```bash
set -a; . ./.env.e2e; set +a; PLAYWRIGHT_BASE_URL=http://localhost:3000 \
  npx playwright test --project=chromium --workers=2
```

## 4. 운영 반영

```bash
npm run seed:magazine-<name>          # .env.local = 운영 Neon, 즉시 라이브
```

코드 변경이 있으면 커밋 → `git push origin master` → Vercel 자동 배포.

## 5. 운영 검증

```bash
PLAYWRIGHT_BASE_URL=https://www.medirok.com npx playwright test \
  e2e/seo.spec.ts e2e/home.spec.ts e2e/hospitals.spec.ts \
  e2e/local-seo.spec.ts e2e/magazine.spec.ts e2e/static-pages.spec.ts \
  --project=chromium --workers=1
```

> ⚠ `e2e/magazine.spec.ts`는 **매거진 1편만 샘플링**해 검사한다(`e2e/.state.json`의 `magazineSlug`). 신규 발행 후에는 전체 스캔을 한 번 돌려 원문 노출을 확인할 것 — §7 스니펫 참고.

## 6. 색인 요청

```bash
npm run indexnow -- --dry-run /magazine/<slug>     # 페이로드·URL 상태만 확인
npm run indexnow -- /magazine/<slug>               # 실제 제출 (202 정상)
```

- 키 파일: `public/0bdf2a333d270c77d8a632f9455079de.txt` → `https://www.medirok.com/<key>.txt`가 200이어야 한다. 스크립트가 전송 전 자동 확인한다.
- IndexNow = Bing 계열. **ChatGPT 검색이 Bing 인덱스를 쓰므로 인용의 전제 조건.**
- **Google Search Console은 수동** (API 자격증명 없음): URL 검사 → 색인 생성 요청. Gemini 대응.

## 7. 전체 매거진 원문 노출 스캔

```bash
curl -s https://www.medirok.com/sitemap.xml | grep -oE '<loc>[^<]*/magazine/[a-z0-9가-힣-]+</loc>' | sed 's/<[^>]*>//g' | while read -r u; do
  out=$(curl -s "$u" | python3 -c "
import sys,re,html
h=sys.stdin.read(); m=re.search(r'<article.*?</article>',h,re.S)
if not m: print('NO-ARTICLE'); raise SystemExit
t=html.unescape(re.sub(r'<[^>]+>',' ',m.group(0))); bad=[]
for pat,name in [(r'^#{1,4}\s','heading'),(r'\*\*[^*]+\*\*','bold'),(r'^\|.+\|.+\|\$','table'),(r'\[[^\]]+\]\(https?://','link')]:
    if re.search(pat,t,re.M): bad.append(name)
print(','.join(bad) if bad else 'ok')")
  printf "%-6s %s\n" "$out" "${u##*/magazine/}"
done
```

---

## 8. 실행 기록 — 2026-07 W3 발행팩 (2026-07-26)

발행팩 4편 중 **01·03 운영 반영 완료**, 02·04는 `publishedAt: 2026-08-02`로 대기.

| 글 | slug | 상태 |
|---|---|---|
| 01 인천 라미네이트 | `guide-incheon-laminate-2026` | ✅ 라이브 |
| 03 청담 시술별 | `guide-cheongdam-derma-by-procedure-2026` | ✅ 라이브 |
| 02 인천 광역 | `guide-incheon-dental-2026` | ⏳ 스크립트 준비 완료 |
| 04 첨단재생의료 | `guide-advanced-regenerative-medicine-clinic` | ⏳ 스크립트 준비 완료 |

- 사실 검증: `[확인]` 항목은 `seed-yeon.ts`·`seed-dod.ts` 검증분으로만 채우고 태그 제거(진료시간·전문의 구성은 구체화). 원고 02의 구별 밀집도·야간진료 비율 주장, "검단은 신규 개원이라 시설 최신", 원고 03 도입부 "밀집도 국내 최고"는 삭제.
- 발견·수정한 배관 문제 (커밋 `6916d2c`):
  - `Markdown.tsx` — 본문의 `https://www.medirok.com/...` 자사 링크가 전부 `rel="nofollow" target="_blank"` 처리되고 있었다. 발행팩 §2 링크 지침이 절대 URL을 쓰라고 되어 있어 그대로 따랐으면 신규 글도 동일 상태. 호스트 기준 판정으로 교체, 기존 3편도 정상화.
  - `hospital/[slug]` — 매거진 역링크가 `authorDoctor` 기준으로만 조회돼 저자가 큐레이션팀인 글은 하나도 걸리지 않았다(체크리스트 5번 사실상 미구현). 죽어 있던 `getMagazinesByHospital`을 합쳐 dedupe, 섹션명 "관련 메디록 매거진"으로 변경.
- canonical 정렬 (커밋 `07d024a`): apex→www 308을 실측하고 `SITE_URL`을 www로 변경. `e2e/seo.spec.ts`의 하드코딩 apex 단언은 `SITE_URL` import로 교체.
- 마크다운 버그 (커밋 `578cf97`): 원고 03의 `'**에서만`이 렌더되지 않아 운영 스모크에서 검출 → 따옴표 제거 후 재시드. 운영 15편 전체 스캔 `ok`.
- 검증 결과: 로컬 e2e **68 passed / 4 skipped**, 운영 스모크 **57 passed / 1 failed → 수정 후 magazine.spec 15 passed**.
- IndexNow: 01·03 제출 **202 Accepted**. GSC 색인 요청은 **미수행(수동 필요)**.

### 다음 주(2026-08-02) 실행할 것

1. `npm run seed:magazine-incheon-dental`
2. `npm run seed:magazine-regenerative`
3. **03번 글에 04번 내부 링크 추가** — `scripts/seed-magazine-cheongdam-procedure.ts` body의 "첨단재생의료실시기관" 첫 등장에 `[...](/magazine/guide-advanced-regenerative-medicine-clinic)`를 걸고 `npm run seed:magazine-cheongdam-procedure` 재실행 (발행팩 §5 링크 지침. 지금 걸면 미발행 URL이라 404여서 보류한 것)
4. §7 전체 스캔 → §5 운영 스모크 → `npm run indexnow -- <두 URL>`
5. GSC URL 검사 → 색인 생성 요청 ×4 (01·03 포함)

### 남은 이슈 (별도 판단)

- `articleSchema`의 `publisher.logo.url = ${SITE_URL}/logo.png` — 저장소에 `logo.png`가 없어 404. 로고 파일을 `public/`에 추가하거나 필드를 제거해야 한다. **`blogPostingSchema`도 같은 값을 쓰므로 함께 해결된다.**
- OG 이미지 없음 — 매거진·블로그 공유 시 썸네일 미노출.
- 사이트맵의 `/hospitals/역/...` URL이 운영에서 404 (`e2e/hospitals.spec.ts:174` test.fixme). 사이트맵이 깨진 URL을 광고 중.

---

## 9. 병원 블로그 (`/blog` — 네이버 블로그 이식, enterprise)

병원 공식 네이버 블로그의 포스팅을 **주제별로 재구성**해 싣는 기능. 네이버 원본은 그대로 두고 메디록이 원출처를 명시한다. 매거진과 컬렉션·라우트가 분리돼 있다.

### 9-1. 매거진과 다른 점만

| 항목 | 매거진 | 병원 블로그 |
|---|---|---|
| 컬렉션 | `magazines` | `blog-posts` |
| URL | `/magazine/<slug>` | `/blog/<병원slug>/<slug>` (3레벨) |
| 병원 관계 | `linkedHospitals` (hasMany, 선택) | `hospital` (단수, **필수**) — URL 1단계 |
| 원출처 | 없음 | `sourceBlogName`·`sourceBlogUrl`·`sourcePosts[]` **필수** |
| JSON-LD | `Article` | `BlogPosting` + `isBasedOn`/`citation` |
| 시드 | `seed:magazine-*` | `seed:blog-posts` (3편 일괄) |
| 원고 문서 | `docs/매거진-*.md` | `docs/블로그-*.md` |

### 9-2. 집필 원칙 (§1에 더해)

- **1:1 복제 금지.** 한 편은 원문 여러 편의 종합이고, 근거가 된 원문은 `sourcePosts`에 **전부** 기재한다. 누락하면 출처 없이 옮긴 것이 된다.
- 의료 서술은 병원이 직접 작성·공개한 원문의 것을 유지한다(원출처가 병원이므로). 단 **최상급·타 의료기관 대비 우위 주장은 옮기지 않는다** — §1의 사실 원칙 그대로.
- 병원 시설·진료시간·주소는 원문이 아니라 `seed-yeon.ts`/`seed-dod.ts` 검증분을 쓴다. 원문끼리 표기가 엇갈리는 경우가 있다(예온 원문의 "검단구" vs "서구").
- `authorDoctor`는 **원장 1인칭 글일 때만** 설정한다. JSON-LD `author`가 Person↔Organization으로 갈리고 저자 프로필 박스 노출도 달라진다.
- 이미지는 쓰지 않는다(v1). 네이버 이미지(`pstatic.net`)는 Referer 차단으로 핫링크가 깨지고, 자체 호스팅 파이프라인이 없다.
- 기존 매거진과 키워드가 겹치면 **검색의도로 역할을 분리**한다 — 매거진은 지역/시술 선택 기준, 블로그는 병원·원장 관점의 진료 서술. 상호 크로스링크로 묶는다.

### 9-3. 신규 컬렉션 추가 시 (스키마 변경이 있을 때만)

```bash
npx payload migrate:create <name>            # 로컬. 산출 SQL이 additive인지 반드시 눈으로 확인
# → src/migrations/index.ts에 자동 등록됨 (확인)

# 운영 적용 — pooled가 아니라 UNPOOLED로, push는 명시적으로 차단
DATABASE_URL="$(node --env-file=.env.local -p 'process.env.DATABASE_URL_UNPOOLED')" \
PAYLOAD_DB_PUSH=false \
node --env-file=.env.local node_modules/payload/bin.js migrate:status   # 먼저 상태 확인
# ... migrate 로 교체해 실행 후 다시 status
```

> `.env.local`은 값에 `&`가 있어 **셸 `. ./.env.local` 소싱이 깨진다.** 위처럼 `node --env-file=`로 읽을 것.

**순서 고정: 마이그레이트 → 코드 배포 → 시드.** 코드가 먼저 배포되면 `force-dynamic`인 `/blog`가 테이블 부재로 500난다. 마이그레이트만 먼저 도는 것은 무해하다(구 코드는 새 테이블을 모른다). 배포~시드 사이 `/blog`는 빈 상태 UI로 200을 반환한다.

### 9-4. 실행 기록 — 파일럿 3편 (2026-08-01)

| 글 | URL | 원문 |
|---|---|---|
| 예온 · 인천 치과 | `/blog/예온치과병원/yeon-incheon-dental` | income3357 3편 |
| 예온 · 인천 라미네이트 | `/blog/예온치과병원/yeon-incheon-laminate` | income3357 3편 |
| 디오디 · 청담 피부과 | `/blog/디오디피부과의원청담/dod-cheongdam-skin` | tj32xdcbiswj9 4편 (이준 원장 명의) |

- 원문 조사: Playwright로 두 블로그 전체 목록 수집(예온 488편·디오디 190편) 후 주제 매칭 후보 10편 본문 추출 → 3편으로 재구성.
- 실행 순서: 마이그레이션(batch 2, additive) → master 배포(`243597c`) → 시드. 배포~시드 사이 `/blog`는 빈 상태 200 확인.
- 검증: 로컬 e2e **84 passed / 4 skipped**(블로그 17건 신규), 운영 스모크 **43 passed / 1 skipped**, 3편 마크다운 누출 스캔 `ok`.
- JSON-LD 실측: 예온 = `author` Organization:예온치과병원 / isBasedOn·citation 3, 디오디 = `author` Person:이준 / isBasedOn·citation 4. 병원별 목록은 `Blog` + `sameAs`(네이버 원본).
- 사이트맵 73 → **79 URL**. IndexNow 6건 제출 **200**. GSC 색인 요청은 **미수행(수동 필요)**.
- 발견·고친 것: 최초 구현에서 `authorName`("메디록 큐레이션팀")이 JSON-LD `author`로 나가 병원 URL과 어긋났다. 저자는 **원문 작성 주체**여야 하므로 `authorDoctor ?? hospital`로 교체하고 시드의 `authorName`을 비웠다. 재구성 주체는 `publisher`와 출처 박스가 밝힌다.

### 9-5. 로컬 검증 시 주의

- dev 서버 포트 3000이 **다른 프로젝트에 점유**돼 있으면 Playwright의 `reuseExistingServer`가 그 서버를 붙잡아 `/api/*`가 404난다. 포트를 확인하고(`lsof -ti:3000`) 3001로 띄운 뒤 `PLAYWRIGHT_BASE_URL`을 넘길 것.
- `npm ci` 이후 Playwright 브라우저 빌드 번호가 어긋나면(`chromium_headless_shell-XXXX 없음`) `npx playwright install chromium`.
- `npm run e2e:seed`는 seed-e2e → seed-yeon → seed-dod → seed-blog-posts 체인이다. 블로그 글의 `hospital`이 필수라 병원 시드가 선행돼야 한다.
