# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage
/playwright-report/
/test-results/
/blob-report/
/e2e/.state.json

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files
.env*
!.env.example
!.env.e2e

# typescript
*.tsbuildinfo
next-env.d.ts

# database
*.db
*.db-journal

# 썸네일 작업 디렉터리 (수집 원본·생성 카드 — 최종본은 Blob에 올라간다)
/.thumbnails/
# Media 로컬 디스크 폴백 (BLOB_READ_WRITE_TOKEN 없는 환경의 업로드 경로)
/media/

.vercel

# claude code (로컬 에이전트 설정)
.claude/
